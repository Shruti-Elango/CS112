
''' Purpose: demonstrate the printing of a string of characters
'''

# get a string 
reply = input( 'Enter a string: ' )

### print the characters

for ch in reply:
    #1st time: ch = first character in reply = [0]
    hmmm = ch + hmmm
    # here we are building the reverse of the origonal string
    # we are sticking each character at the begining of the accumlator
    print( hmmm )

    # if reply = 'apple'
    #cha = 'a'

...
