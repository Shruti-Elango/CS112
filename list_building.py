
''' Support your understanding of lists
        list building
'''


# get some lists

print()

# create and add to values
values = [ ] #initiating our empty list --> accumulator --> accumulate element to this list

#
values.append( "u" )
values.append( "v" )
values.append( "a" )
values.append( "!" )
values.append( " " )
values.append( "u" )
values.append( "v" )
values.append( "a" )
values.append( "!" )

print( "values  =", values )

input('Enter when ready:') # hits enter key when you reach this point

# create and add to ints
ints = []

for i in range( 0, 10 ) :
    ints.append( i )

print( "ints =", ints ) # indent the print statement here! this will print the list ints for each
# run of the for loop

# print ('ints=', ints) # we print the final version of the list inst once afterthe for loop is done

#this is the same as the first few lines


s= 'uva! uva!'
letters= []
for ch in s:
    letters.append(ch)
    print(letter)
